<% if (messages.errors || messages.notice) { %>
  <div class="alert alert-<%= messages.errors ? 'danger' : 'warning' %> alert-dismissible fade show" role="alert">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>

    <% if (messages.errors) { %>
    Validation errors:
    
    <ul>
      <% messages.errors.forEach((err) => { %>
        <li>
          <%= `${err.param} ${err.msg}` %>
        </li>
      <% }) %>
    </ul>
    <% } else { %>
      <p><%= messages.notice %></p>
    <% } %>
  </div>
<% } %>